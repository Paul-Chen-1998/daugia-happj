

import 'package:flutter/material.dart';

class PageHoSo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: new Scaffold(
        body: new Center(child:new Text('Ho so')),
      ),
    );
  }
}
